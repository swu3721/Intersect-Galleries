# Copy to .env in this folder for local Edge Functions (never commit .env).
# Create a Spotify app: https://developer.spotify.com/dashboard → Client ID + Client Secret
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
